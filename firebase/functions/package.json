{
  "name": "caocap-functions",
  "private": true,
  "engines": {
    "node": "22"
  },
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc",
    "test": "npm run build && node --test tests/unit.test.cjs",
    "test:rules": "node --test tests/rules.test.cjs",
    "test:integration": "npm run build && node --test tests/integration.test.cjs"
  },
  "dependencies": {
    "firebase-admin": "^13.6.0",
    "firebase-functions": "^6.6.0"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.2",
    "@types/node": "^22.18.0",
    "firebase": "^12.18.0",
    "typescript": "^5.9.2"
  }
}
